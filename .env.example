# GitHub Personal Access Token
# Required for full GitHub stats features (contribution graph, commits, PRs, issues, pinned repos)
# Without this token, the site will still work with basic stats (stars, forks, top repos)
#
# How to create:
# 1. Go to https://github.com/settings/tokens?type=beta
# 2. Click "Generate new token"
# 3. Name: "portfolio-stats" (or anything you like)
# 4. Expiration: 1 year recommended
# 5. Permissions: Only "Public Repositories (read-only)" is needed
# 6. Generate and copy the token below
#
# For Vercel deployment:
# Add this as an environment variable in your Vercel project settings
# https://vercel.com/docs/concepts/projects/environment-variables

GITHUB_TOKEN=
